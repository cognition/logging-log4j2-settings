# Prometheus JMX Exporter config for YARN NodeManager
# Use with: -javaagent:jmx_prometheus_javaagent.jar=9093:this-file
# See docs/JMX_EXPORTER_SETUP.md

lowercaseOutputName: true
lowercaseOutputLabelNames: true

whitelistObjectNames:
  - "java.lang:*"
  - "Hadoop:service=NodeManager,name=*"
